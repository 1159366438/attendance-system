前后后端交互问题
统一前后端的返回值格式
{
  "code": 200,    // 业务状态码，200成功，非200失败
  "msg": "success", // 提示信息
  "data": { ... }  // 真正的业务数据
}
在组件里判断业务状态码，根据后端返回的code判断

今天的工作进展：
1. 实现了前端组件中对后端返回业务状态码的判断逻辑
2. 根据后端返回的code字段进行不同的处理
3. 完善了错误处理机制，提升用户体验